'use strict';

exports.__esModule = true;

var _helpers = require('./internals/helpers');

/**
 * Invoke function taked from result
 */
exports.default = (0, _helpers.defFactory)('invoke', function (fn) {
  return function invoke() {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    var payload = fn.apply(this, args);
    if ((0, _helpers.isFunction)(payload)) {
      return payload.apply(this, args);
    } else if ((0, _helpers.isArray)(payload) && (0, _helpers.isFunction)(payload[0])) {
      return payload[0].apply(payload, payload.slice(1));
    }
    return payload;
  };
});
module.exports = exports['default'];