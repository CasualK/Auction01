'use strict';

exports.__esModule = true;

var _helpers = require('./internals/helpers');

exports.default = (0, _helpers.defFactory)('whileTruly', function whileTruly() {
  var _ref;

  for (var _len = arguments.length, series = Array(_len), _key = 0; _key < _len; _key++) {
    series[_key] = arguments[_key];
  }

  var nextName = 'whileTrulySerie' + series.length;
  var _ = (_ref = {}, _ref[nextName] = function () {
    var result = void 0;

    for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }

    var nextArgs = args.slice();
    for (var i = 0; i < series.length; i++) {
      result = series[i].apply(this, nextArgs);
      if (!result) {
        break;
      }
      nextArgs = [result];
    }
    return result;
  }, _ref);

  return _[nextName];
});
module.exports = exports['default'];